#include <stdio.h>
/**
 * main - Entry point
 *
 * Description: print a string
 *
 * Return: 0
 **/
int main(void)
{
	char strg[] = "with proper grammar, but the outcome is a piece of art,";

	printf("%s", strg);

	return (0);
}
