#include "main.h"

/**
 * _strlen - Function
 *
 * @s: Write a function that returns the length of a string.
 * Return: i
 **/

int _strlen(char *s)

{
	int i;

	for (i = 0; i <= *s; i++)
	{
		;
	}
		return (i);
}
