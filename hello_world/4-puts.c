#include <stdio.h>
/**
 * main - Entry point
 * strg : string
 *
 * Return: 0
 **/

int main(void)
{
	char strg[] = "\"Programming is like building a multilingual puzzle";

	puts(strg);

	return (0);
}
