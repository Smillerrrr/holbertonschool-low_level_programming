#include "main.h"

/**
 * _isdigit - Write a function that checks for a digit
 *
 * @c: print a number
 * Return: 0 or 1
 **/

int _isdigit(int c)

{
	if (c <= 9)

		return (1);

		else

		return (0);
}
